# Optional. Without a key the app runs a deterministic rule-based agent that
# drives exactly the same tools, so everything below still works offline.
ANTHROPIC_API_KEY=

# Optional overrides
# ANTHROPIC_MODEL=claude-opus-5
# ANTHROPIC_EFFORT=low
# PORT=3001
